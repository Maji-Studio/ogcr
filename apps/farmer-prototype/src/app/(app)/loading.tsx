/**
 * Loading state for app routes
 */
export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="body-large text-[var(--color-text-secondary)]">Loading…</p>
    </div>
  );
}
