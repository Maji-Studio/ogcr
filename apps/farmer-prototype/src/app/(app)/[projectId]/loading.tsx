/**
 * Loading state for project-scoped routes (renders inside the sidebar layout)
 */
export default function Loading() {
  return (
    <p className="body-large text-[var(--color-text-secondary)]">Loading…</p>
  );
}
